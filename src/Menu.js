import "./App.css";

const Menu = (props) => (
  <div>
    <div>
      <div 
    //   style={{justifyContent: "left"}}
      >{props.mass1}</div>
      <div 
    //   style={{justifyContent: "right"}}
      >{props.mass2}</div>
    </div>
  </div>
);

export default Menu;
